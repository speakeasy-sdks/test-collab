openapi: 3.0.1
info:
  title: Prove Auth API
  description: |-
    This specification describes the Prove Auth API.

    Swagger 2.0 Spec - generated by [go-swagger](https://github.com/go-swagger/go-swagger)
  license:
    name: Copyright © Prove. All rights reserved.
  version: 1.37.3
servers:
  - url: https://oapi.uat.prove-auth.proveapis.com/
paths:
  /v1/server/auth/continue:
    post:
      tags:
        - Auth
      summary: AuthContinue /v1/server/auth/continue
      description: |-
        If the initial Prove Auth authenticators fail, the Force Bind authenticator can be used which permits using another
        authentication method to verify a mobile number (like Prove Instant Link™). Once the mobile number is verified, send
        this AuthContinue request.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/auth/continue
      operationId: AuthContinueRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthContinueRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthContinueResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/auth/exchange:
    post:
      tags:
        - Auth
      summary: ServerAuthExchange /v1/server/auth/exchange
      description: Exchange AuthID for AuthToken.
      operationId: AuthExchangeRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthExchangeRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthExchangeResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/auth/finish:
    post:
      tags:
        - Auth
      summary: AuthFinish /v1/server/auth/finish
      description: |-
        To complete the auth flow and get the authentication result, send this AuthFinish request.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/auth/finish
      operationId: AuthFinishRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthFinishRequest"
        required: false
      responses:
        "200":
          description: Result of the auth flow.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthFinishResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/auth/start:
    post:
      tags:
        - Auth
      summary: AuthStart /v1/server/auth/start
      description: |-
        To start an auth flow, send this AuthStart request to control how Prove Auth will authenticate the end users and
        their devices. The expected authenticators should be included in the body parameters. There are many supported
        scenarios to use Prove Auth so each of the request types are explained in the "Server Integration Guide".

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/auth/start
      operationId: AuthStartRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AuthStartRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AuthStartResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/device/unregister:
    post:
      tags:
        - Device
      summary: ServerDeviceUnregister /v1/server/device/unregister
      description: |-
        Mark a device as inactive so it can no longer be used in an auth flow.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/device/unregister
      operationId: DeviceUnregisterRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeviceUnregisterRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeviceUnregisterResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/push/delete:
    post:
      tags:
        - Push
      summary: ServerPushTokenDelete /v1/server/push/delete
      description: |-
        Delete push tokens for a list of devices so they will no longer receive push notifications.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/push/delete
      operationId: PushTokenDeleteRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushTokenDeleteRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PushTokenDeleteResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/user/info:
    post:
      tags:
        - Push
      summary: ServerUserInfo /v1/server/user/info
      description: |-
        Return device records associated with a user ID.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/user/info
      operationId: UserInfoRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserInfoRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserInfoResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/user/mobileactive:
    post:
      tags:
        - Push
      summary: ServerUserMobileActive /v1/server/user/mobileactive
      description: |-
        Set user ID as active on their associated device.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/user/mobileactive
      operationId: UserMobileActiveRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserMobileActiveRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserMobileActiveResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/user/remove:
    post:
      tags:
        - Push
      summary: ServerUserRemove /v1/server/user/remove
      description: |-
        Deactivate user ID and associated devices.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/user/remove
      operationId: UserRemoveRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserRemoveRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserRemoveResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
  /v1/server/user/unbind:
    post:
      tags:
        - Push
      summary: ServerUserUnbind /v1/server/user/unbind
      description: |-
        Deactivate user ID on either a specified device or all associated devices. Device(s) will remain active.

        Production URL: https://oapi.prove-auth.proveapis.com/v1/server/user/unbind
      operationId: UserUnbindRequest
      requestBody:
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserUnbindRequest"
        required: false
      responses:
        "200":
          description: Result of the operation.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserUnbindResponse"
        "400":
          description:
            Bad Request. The server cannot process the request due to a
            client error.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
        "500":
          description:
            Internal Server Error. The server encountered an unexpected
            condition that prevented it from fulfilling the request.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
      security:
        - token: []
      x-codegen-request-body-name: Body
components:
  schemas:
    AuthContinueRequest:
      required:
        - authId
        - requestId
        - subjects
      type: object
      properties:
        authId:
          type: string
          description: AuthID is the unique ID of the auth flow to continue.
          example: 713189b8-5555-4b08-83ba-75d08780aebd
          x-go-name: AuthID
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
        subjects:
          $ref: "#/components/schemas/AuthContinueRequestSubjects"
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueRequestSubjectMobile:
      type: object
      properties:
        claim:
          $ref: "#/components/schemas/AuthContinueRequestSubjectMobileClaim"
      x-go-name: SubjectMobile
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueRequestSubjectMobileClaim:
      required:
        - mobileNumber
      type: object
      properties:
        mobileNumber:
          type: string
          description: |-
            MobileNumber to bind to the device. Mobile number may be prefixed with a plus sign, but does not need to be for
            US. A plus sign is required for international.
          example: "12065550100"
          x-go-name: MobileNumber
      x-go-name: SubjectMobileClaim
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueRequestSubjects:
      type: object
      properties:
        mobile:
          $ref: "#/components/schemas/AuthContinueRequestSubjectMobile"
      x-go-name: Subjects
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueResponse:
      required:
        - subjects
      type: object
      properties:
        subjects:
          $ref: "#/components/schemas/AuthContinueResponseSubjectsResults"
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueResponseError:
      required:
        - message
      type: object
      properties:
        code:
          type: integer
          description:
            Code is an internal error code that describes the problem category
            of the request.
          format: int64
          example: 10004
          x-go-name: Code
        message:
          type: string
          description:
            Message is an error message describing the problem with the
            request.
          example: device authentication missing
          x-go-name: Message
      x-go-name: Error
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueResponseSubjectCardResult:
      type: object
      properties:
        error:
          $ref: "#/components/schemas/AuthContinueResponseError"
        success:
          type: boolean
          x-go-name: Success
      x-go-name: SubjectCardResult
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueResponseSubjectMobileResult:
      required:
        - success
      type: object
      properties:
        error:
          $ref: "#/components/schemas/AuthContinueResponseError"
        success:
          type: boolean
          description: Success will be true if the bind was successful.
          example: true
          x-go-name: Success
      x-go-name: SubjectMobileResult
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthContinueResponseSubjectsResults:
      type: object
      properties:
        card:
          $ref: "#/components/schemas/AuthContinueResponseSubjectCardResult"
        mobile:
          $ref: "#/components/schemas/AuthContinueResponseSubjectMobileResult"
      x-go-name: SubjectsResults
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_continue
    AuthExchangeRequest:
      required:
        - authId
      type: object
      properties:
        authId:
          type: string
          description:
            AuthID is the unique ID of the auth flow to exchange for a
            token.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: AuthID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_exchange
    AuthExchangeResponse:
      required:
        - authToken
      type: object
      properties:
        authToken:
          type: string
          description: AuthToken is a bearer token for use by the Prove Auth SDK.
          example: eyJhbGciOi...
          x-go-name: AuthToken
        error:
          $ref: "#/components/schemas/Error"
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_exchange
    AuthFinishRequest:
      required:
        - authId
      type: object
      properties:
        authId:
          type: string
          description: AuthID is the unique ID of the auth flow to retrieve the result.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: AuthID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponse:
      required:
        - appNamespace
        - authId
        - subjects
      type: object
      properties:
        appNamespace:
          type: string
          description: AppNamespace describes the Prove Auth environment.
          example: prod
          x-go-name: AppNamespace
        authId:
          type: string
          description: AuthID is the unique ID of the auth flow.
          example: 713189b8-5555-4b08-83ba-75d08780aebd
          x-go-name: AuthID
        delivery:
          $ref: "#/components/schemas/AuthFinishResponseDelivery"
        subjects:
          $ref: "#/components/schemas/AuthFinishResponseSubjects"
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseCardError:
      required:
        - message
      type: object
      properties:
        code:
          type: integer
          description:
            Code is an internal error code that describes the problem category
            of the request.
          format: int64
          example: 10004
          x-go-name: Code
        message:
          type: string
          description:
            Message is an error message describing the problem with the
            request.
          example: device authentication missing
          x-go-name: Message
        puid:
          type: string
          description:
            PUID is the unique ID associated with an AirKey card that was
            extracted from the AirKey message.
          x-go-name: PUID
      x-go-name: CardError
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseDelivery:
      type: object
      properties:
        push:
          $ref: "#/components/schemas/AuthFinishResponsePushDelivery"
        scan:
          $ref: "#/components/schemas/AuthFinishResponseScanDelivery"
      x-go-name: Delivery
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseError:
      required:
        - message
      type: object
      properties:
        code:
          type: integer
          description:
            Code is an internal error code that describes the problem category
            of the request.
          format: int64
          example: 10004
          x-go-name: Code
        message:
          type: string
          description:
            Message is an error message describing the problem with the
            request.
          example: device authentication missing
          x-go-name: Message
      x-go-name: Error
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponsePushDelivery:
      type: object
      properties:
        extras:
          $ref: "#/components/schemas/AuthFinishResponsePushDeliveryExtras"
        response:
          type: string
          x-go-name: UserResponse
      x-go-name: PushDelivery
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponsePushDeliveryExtras:
      type: object
      properties:
        distanceFromOriginatingIp:
          type: integer
          format: int64
          x-go-name: DistanceFromOriginatingIp
        originatingIpGeolocation:
          type: string
          x-go-name: OriginatingIpGeolocation
      x-go-name: PushDeliveryExtras
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseScanDelivery:
      type: object
      properties:
        extras:
          $ref: "#/components/schemas/AuthFinishResponseScanDeliveryExtras"
        response:
          type: string
          x-go-name: UserResponse
      x-go-name: ScanDelivery
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseScanDeliveryExtras:
      type: object
      properties:
        originatingIpGeolocation:
          type: string
          x-go-name: OriginatingIpGeolocation
      x-go-name: ScanDeliveryExtras
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectDeviceResult:
      type: object
      properties:
        error:
          $ref: "#/components/schemas/AuthFinishResponseError"
        success:
          $ref: "#/components/schemas/AuthFinishResponseSubjectDeviceResultSuccess"
      x-go-name: SubjectDeviceResult
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectDeviceResultSuccess:
      type: object
      properties:
        claim:
          $ref: "#/components/schemas/AuthFinishResponseSubjectDeviceResultSuccessClaim"
        createdAt:
          type: integer
          description:
            "CreatedAt field is in Unix time which is the number of seconds\
            \ elapsed since January 1, 1970 UTC."
          format: int64
          example: 1666394795
          x-go-name: CreatedAt
        extras:
          $ref: "#/components/schemas/AuthFinishResponseSubjectDeviceResultSuccessExtras"
        uvLevel:
          type: integer
          description:
            UserVerificationLevel is the level that was performed during
            device passive authentication.
          format: int64
          x-go-name: UserVerificationLevel
      x-go-name: SubjectDeviceResultSuccess
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectDeviceResultSuccessClaim:
      required:
        - deviceId
      type: object
      properties:
        deviceId:
          type: string
          description: DeviceID is the unique ID of the device.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: DeviceID
      x-go-name: SubjectDeviceResultSuccessClaim
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectDeviceResultSuccessExtras:
      type: object
      properties:
        deviceCapabilities:
          type: array
          description:
            "DeviceCapabilities contains the available device capabilities\
            \ (webauthn, face, fingerprint)."
          items:
            type: string
          x-go-name: DeviceCapabilities
        deviceType:
          type: string
          description:
            "DeviceAuthType contains description of the type of client\
            \ device used for auth: 'web' or 'mobile'."
          x-go-name: DeviceAuthType
        supportedUvLevel:
          type: array
          description:
            SupportedUvLevel indicates the level of user verification supported
            on the device.
          items:
            type: integer
            format: int64
          x-go-name: SupportedUvLevel
        trust:
          $ref: "#/components/schemas/SubjectDeviceResultSuccessExtrasTrust"
      x-go-name: SubjectDeviceResultSuccessExtras
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectMobileResult:
      type: object
      properties:
        error:
          $ref: "#/components/schemas/AuthFinishResponseError"
        success:
          $ref: "#/components/schemas/AuthFinishResponseSubjectMobileResultSuccess"
      x-go-name: SubjectMobileResult
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectMobileResultSuccess:
      required:
        - authenticatorType
      type: object
      properties:
        authenticatorType:
          type: string
          description: |-
            AuthenticatorType specifies the type of authenticator used in the auth flow. Values: instant, passive, and
            provided.
          example: instant
          x-go-name: AuthenticatorType
        claim:
          $ref: "#/components/schemas/AuthFinishResponseSubjectMobileResultSuccessClaim"
        createdAt:
          type: integer
          description:
            "CreatedAt field is in Unix time which is the number of seconds\
            \ elapsed since January 1, 1970 UTC."
          format: int64
          example: 1666394795
          x-go-name: CreatedAt
        extras:
          $ref: "#/components/schemas/AuthFinishResponseSubjectMobileResultSuccessExtras"
      x-go-name: SubjectMobileResultSuccess
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectMobileResultSuccessClaim:
      required:
        - mobileNumber
      type: object
      properties:
        mobileNumber:
          type: string
          description: |-
            MobileNumber bound to the device. Mobile number may be prefixed with a plus sign, but does not need to be for
            US. A plus sign is required for international.
          example: "12065550100"
          x-go-name: MobileNumber
      x-go-name: SubjectMobileResultSuccessClaim
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjectMobileResultSuccessExtras:
      type: object
      properties:
        mobileCountryCode:
          type: string
          description: MobileCountryCode is the country code of the mobile number.
          example: "310"
          x-go-name: MobileCountryCode
        mobileOperatorName:
          type: string
          description:
            MobileOperatorName is the name of the mobile network operator
            of the mobile number.
          example: Verizon
          x-go-name: MobileOperatorName
        payfoneAlias:
          type: string
          description: PayfoneAlias is the Prove alias of the mobile number.
          example: 37C2666C4VKR...
          x-go-name: PayfoneAlias
      x-go-name: SubjectMobileResultSuccessExtras
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthFinishResponseSubjects:
      type: object
      properties:
        card:
          $ref: "#/components/schemas/SubjectCardResult"
        device:
          $ref: "#/components/schemas/AuthFinishResponseSubjectDeviceResult"
        mobile:
          $ref: "#/components/schemas/AuthFinishResponseSubjectMobileResult"
      x-go-name: Subjects
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    AuthStartRequest:
      required:
        - requestId
        - subjects
      type: object
      properties:
        callbackUrl:
          type: string
          description: |-
            CallbackURL is an optional callback URL (webhook) where Prove Auth will delivery the user's Push Auth response at
            the end of the auth flow. If there are any errors encountered during the auth flow, those will be delivered to
            the callback URL as well. If not provided, the customer server must call the AuthFinish endpoint to retrieve the
            auth results. The request will be sent as a POST and will contain the same response body as the AuthFinish
            response.
          example: https://example.com/webhook
          x-go-name: CallbackURL
        delivery:
          $ref: "#/components/schemas/AuthStartRequestDelivery"
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
        subjects:
          $ref: "#/components/schemas/AuthStartRequestSubjects"
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestDelivery:
      required:
        - push
      type: object
      properties:
        push:
          $ref: "#/components/schemas/AuthStartRequestDeliveryPush"
      x-go-name: Delivery
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestDeliveryPush:
      required:
        - notification
      type: object
      properties:
        notification:
          $ref: "#/components/schemas/AuthStartRequestDeliveryPushNotification"
      x-go-name: DeliveryPush
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestDeliveryPushNotification:
      required:
        - body
        - title
      type: object
      properties:
        body:
          type: string
          description: Body is the body of the push message.
          example: Do you want to allow login from this device?
          x-go-name: Body
        confirmBtn:
          type: string
          description: ConfirmButton is the text of the push message confirm button.
          example: Confirm
          x-go-name: ConfirmButton
        denyBtn:
          type: string
          description: DenyButton is the text of the push message deny button.
          example: Deny
          x-go-name: DenyButton
        originatingDevice:
          type: string
          description:
            OriginatingDevice is the type of device where where the push
            flow was initiated.
          example: iPhone12
          x-go-name: OriginatingDevice
        originatingIp:
          type: string
          description:
            OriginatingIp is the IP address of the web client where the
            push flow was initiated.
          example: 198.51.100.10
          x-go-name: OriginatingIp
        title:
          type: string
          description: Title is the title of the push message.
          example: Confirm Login
          x-go-name: Title
      x-go-name: DeliveryPushNotification
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestInstantAuthConsent:
      required:
        - status
      type: object
      properties:
        collectedTimestamp:
          type: string
          description: |-
            CollectedTimestamp is the date/time when original consent was collected by the client in ISO 8601 format.
            Required if status = optedIn.
          example: 2022-02-17
          x-go-name: CollectedTimestamp
        description:
          type: string
          description: |-
            Description of the type of consent (electronic/paper), use case, and reference to the terms and conditions (T&C)
            version (if applicable). Required if status = optedIn.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: Customer Application Name
          x-go-name: Description
        status:
          type: string
          description: |-
            Status denotes whether consent has been provided by the mobile subscriber for this transaction. Acceptable values
            are: optedIn, optedOut, notCollected, unknown.
          example: optedIn
          x-go-name: Status
        transactionId:
          type: string
          description: |-
            TransactionID uniquely identifies the consent collected by the client. Required if status = optedIn.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: TransactionID
      x-go-name: InstantAuthConsent
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectDevice:
      type: object
      properties:
        authenticators:
          $ref: "#/components/schemas/AuthStartRequestSubjectDeviceAuthenticators"
      x-go-name: SubjectDevice
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectDeviceAuthenticatorPassive:
      type: object
      properties:
        allowPasscodeFallback:
          type: boolean
          description:
            AllowPasscodeFallback specifies whether passcode is allowed
            as a fallback protection for device in-app step-up keys.
          x-go-name: AllowPasscodeFallback
        userVerificationLevel:
          type: string
          description:
            "UserVerificationLevel accepts 'required', 'preferred' or 'discouraged'\
            \ as a value to specify biometric verification with FIDO."
          x-go-name: UserVerificationLevel
      x-go-name: SubjectDeviceAuthenticatorPassive
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectDeviceAuthenticators:
      type: object
      properties:
        passive:
          $ref: "#/components/schemas/AuthStartRequestSubjectDeviceAuthenticatorPassive"
      x-go-name: SubjectDeviceAuthenticators
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectMobile:
      type: object
      properties:
        authenticators:
          $ref: "#/components/schemas/AuthStartRequestSubjectMobileAuthenticators"
        claim:
          $ref: "#/components/schemas/AuthStartRequestSubjectMobileClaim"
      x-go-name: SubjectMobile
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectMobileAuthenticatorInstant:
      type: object
      properties:
        consent:
          $ref: "#/components/schemas/AuthStartRequestInstantAuthConsent"
        testMode:
          $ref: "#/components/schemas/MobileAuthTestMode"
      x-go-name: SubjectMobileAuthenticatorInstant
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectMobileAuthenticatorOTP:
      required:
        - messageText
      type: object
      properties:
        allowOtpRetry:
          type: boolean
          description: |-
            If AllowOtpRetry is set to true, the end user will have up to 3 OTP entry attempts in a Prove Auth flow. If
            false, the end user will only get 1 OTP attempt. Once the maximum number of attempts is reached, the auth flow
            will return an error.
          example: true
          x-go-name: AllowOtpRetry
        messageText:
          type: string
          description: |-
            MessageText is the message that will be sent to end user with the one-time passcode (OTP) represented by pound
            signs. The number of pound signs should be equal to the OTP length
          example: "Your pin is: ####"
          x-go-name: MessageText
        testMode:
          $ref: "#/components/schemas/OTPTestMode"
      x-go-name: SubjectMobileAuthenticatorOTP
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectMobileAuthenticatorPassive:
      type: object
      properties:
        cacheResult:
          type: boolean
          description:
            "If true, bind will persist between authenticators. If false,\
            \ bind will be reattempted on every auth flow."
          example: true
          x-go-name: CacheResult
        localDomain:
          type: boolean
          description:
            "If true, will allow authentication against a local domain\
            \ for testing FIDO."
          example: true
          x-go-name: LocalDomain
        maxAge:
          type: integer
          description: |-
            If not set or set to 0, the age of the bind will be ignored during authentication. If set to -1, then it will
            force a rebind of the mobile number to the device. If set greater than 0, it specifies the maximum age
            (in seconds) of the bind that is allowed or else a rebind occurs during authentication. Ex: 7776000 is 90 days.
          format: int64
          example: 7776000
          x-go-name: MaxAge
        testMode:
          $ref: "#/components/schemas/PassiveAuthTestMode"
      x-go-name: SSubjectMobileAuthenticatorPassive
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectMobileAuthenticators:
      type: object
      properties:
        instant:
          $ref: "#/components/schemas/AuthStartRequestSubjectMobileAuthenticatorInstant"
        otp:
          $ref: "#/components/schemas/AuthStartRequestSubjectMobileAuthenticatorOTP"
        passive:
          $ref: "#/components/schemas/AuthStartRequestSubjectMobileAuthenticatorPassive"
      x-go-name: SSubjectMobileAuthenticators
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectMobileClaim:
      type: object
      properties:
        mobileNumber:
          type: string
          description: |-
            MobileNumber will be used when performing OTP fallback and will not use the optional value provided by the SDK.
            Mobile number may be prefixed with a plus sign, but does not need to be for US. A plus sign is required for
            international.
          example: "12065550100"
          x-go-name: MobileNumber
      x-go-name: SubjectMobileClaim
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectUser:
      required:
        - authenticators
        - claim
      type: object
      properties:
        authenticators:
          $ref: "#/components/schemas/AuthStartRequestSubjectUserAuthenticators"
        claim:
          $ref: "#/components/schemas/AuthStartRequestSubjectUserClaim"
      x-go-name: SubjectUser
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectUserAuthenticatorPassive:
      type: object
      x-go-name: SubjectUserAuthenticatorPassive
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectUserAuthenticators:
      type: object
      properties:
        passive:
          $ref: "#/components/schemas/AuthStartRequestSubjectUserAuthenticatorPassive"
        present:
          $ref: "#/components/schemas/SubjectUserAuthenticatorPresent"
      x-go-name: SubjectUserAuthenticators
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjectUserClaim:
      required:
        - userId
      type: object
      properties:
        provided:
          type: boolean
          description:
            "Provided if true, registers the user ID with a device, otherwise\
            \ it valids the user ID."
          x-go-name: Provided
        userId:
          type: string
          description: |-
            UserID is a user identifier generated on the customer side to be associated with a device.
            Acceptable characters are: alphanumeric with symbols '-._+=/:'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: UserID
      x-go-name: SubjectUserClaim
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartRequestSubjects:
      type: object
      properties:
        device:
          $ref: "#/components/schemas/AuthStartRequestSubjectDevice"
        mobile:
          $ref: "#/components/schemas/AuthStartRequestSubjectMobile"
        user:
          $ref: "#/components/schemas/AuthStartRequestSubjectUser"
      x-go-name: Subjects
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    AuthStartResponse:
      required:
        - authId
        - authToken
      type: object
      properties:
        authId:
          type: string
          description: |-
            AuthID is the unique ID of the auth flow. To complete the auth flow, the `authId` will also be used for the
            AuthFinish request.
          example: 713189b8-5555-4b08-83ba-75d08780aebd
          x-go-name: AuthID
        authToken:
          type: string
          description: AuthToken is a bearer token for use by the Prove Auth SDK.
          example: eyJhbGciOi...
          x-go-name: AuthToken
        pushTokens:
          type: array
          description:
            PushTokens is an array of Push Tokens for the customer to use
            to initiate push notifications via their own service.
          items:
            $ref: "#/components/schemas/PushToken"
          x-go-name: PushTokens
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    CardEligibilityRequest:
      required:
        - cards
      type: object
      properties:
        cards:
          type: array
          description: Cards contains the list AirKey cards eligibility statuses.
          items:
            $ref: "#/components/schemas/CardEligibilityRequestCardEligibilityStatus"
          x-go-name: Cards
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/card_eligibility
    CardEligibilityRequestCardEligibilityStatus:
      required:
        - puid
        - status
      type: object
      properties:
        puid:
          type: string
          description: PUID uniquely identifies the AirKey card.
          example: "0115992309490011"
          x-go-name: PUID
        status:
          type: string
          description: |-
            Status specifies what is the new AirKey card eligibility status. Acceptable values
            are: valid, disabled, resync_required.
          example: disabled
          x-go-name: Status
      x-go-name: CardEligibilityStatus
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/card_eligibility
    CardEligibilityResponse:
      required:
        - success
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        success:
          type: boolean
          description: Success is true if the operation was successful.
          example: true
          x-go-name: Success
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/card_eligibility
    DeviceUnregisterRequest:
      required:
        - deviceId
        - requestId
      type: object
      properties:
        deviceId:
          type: string
          description: DeviceID is the UUID of the device to unregister.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: DeviceID
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/device_unregister
    DeviceUnregisterResponse:
      required:
        - success
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        success:
          type: boolean
          description: Success is true if the operation was successful.
          example: true
          x-go-name: Success
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/device_unregister
    Error:
      title: Error is the response when there is a problem with the request.
      required:
        - message
      type: object
      properties:
        code:
          type: integer
          description:
            Code is an internal error code that describes the problem category
            of the request.
          format: int64
          example: 1000
          x-go-name: Code
        message:
          type: string
          description:
            Message is an error message describing the problem with the
            request.
          example: error occurred
          x-go-name: Message
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/common/model
    MobileAuthTestMode:
      type: string
      description: |-
        MobileAuthTestMode simulates responses. Acceptable values are: off (default), pass, failStart, failFinish, and
        failService.
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/common/model
    OTPTestMode:
      title:
        "OTPTestMode simulates responses. Acceptable values are: off (default),\
        \ pass, failStart, failFinish, and failService."
      type: string
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/common/model
    PassiveAuthTestMode:
      title:
        "PassiveAuthTestMode simulates responses. Acceptable values are: off\
        \ (default) and fail."
      type: string
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/common/model
    PushToken:
      required:
        - deviceId
        - platform
        - pushToken
        - registrationTimestamp
      type: object
      properties:
        deviceId:
          type: string
          description:
            DeviceID is the UUID of the device where the push message will
            be sent.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: DeviceId
        platform:
          type: string
          description: |-
            Platform is the platform of the device were the push message will be received. Possible values: ios, android,
            and web.
          example: web
          x-go-name: Platform
        pushToken:
          type: string
          description:
            PushToken is the push token to use when sending a push message
            to the user.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: PushToken
        registrationTimestamp:
          type: integer
          description: |-
            RegistrationTimestamp is the timestamp when the push token was associated with the device. The field is in Unix
            time which is the number of seconds elapsed since January 1, 1970 UTC.
          format: int64
          example: 1666394795
          x-go-name: RegistrationTimestamp
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    PushTokenDeleteRequest:
      required:
        - devices
        - requestId
      type: object
      properties:
        devices:
          type: array
          description:
            Devices is an array of UUIDs of the devices to delete push
            tokens.
          example:
            - eba12f3a-5555-47bc-b85d-21c0cbc4b973
            - fba12f3a-5555-47bc-b85d-21c0cbc4b974
          items:
            type: string
          x-go-name: Devices
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/push_delete
    PushTokenDeleteResponse:
      required:
        - success
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        success:
          type: boolean
          description: Success is true if the operation was successful.
          example: true
          x-go-name: Success
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/push_delete
    SubjectCardResult:
      type: object
      properties:
        error:
          $ref: "#/components/schemas/AuthFinishResponseCardError"
        success:
          $ref: "#/components/schemas/SubjectCardResultSuccess"
      description: swaggerIGNORE:model AuthFinishResponseSubjectCardsResult
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    SubjectCardResultSuccess:
      type: object
      properties:
        authenticatorType:
          type: string
          x-go-name: AuthenticatorType
        claim:
          $ref: "#/components/schemas/SubjectCardResultSuccessClaim"
        createdAt:
          type: integer
          description:
            "CreatedAt field is in Unix time which is the number of seconds\
            \ elapsed since January 1, 1970 UTC."
          format: int64
          example: 1666394795
          x-go-name: CreatedAt
      description: swaggerIGNORE:model AuthFinishResponseSubjectCardResultSuccess
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    SubjectCardResultSuccessClaim:
      type: object
      properties:
        puid:
          type: string
          x-go-name: PUID
      description: swaggerIGNORE:model AuthFinishResponseSubjectCardResultSuccessClaim
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    SubjectDeviceResultSuccessExtrasTrust:
      type: object
      properties:
        reasonCodes:
          type: array
          description:
            ReasonCodes contains a list of the reason codes detected on
            the device.
          items:
            type: integer
            format: int64
          x-go-name: ReasonCodes
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_finish
    SubjectUserAuthenticatorPresent:
      type: object
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/auth_start
    UserInfoDeviceResponse:
      required:
        - bindingTimestamp
        - deviceId
        - deviceType
        - lastAuthenticated
        - pushEnabled
      type: object
      properties:
        bindingTimestamp:
          type: integer
          description: |-
            BindingTimestamp is when the user was bound to the device. It's in Unix time which is the number of seconds
            elapsed since January 1, 1970 UTC.
          format: int64
          example: 1666394795
          x-go-name: BindingTimestamp
        deviceCapabilities:
          type: array
          description:
            "DeviceCapabilities indicates what capabilities are available\
            \ on the device. Options are: \"webauthn\", \"face\" and \"fingerprint\"\
            ."
          example:
            - webauthn
          items:
            type: string
          x-go-name: DeviceCapabilities
        deviceId:
          type: string
          description: DeviceID is the UUID of the device to unregister.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: DeviceID
        deviceName:
          type: string
          description: DeviceName is the friendly name of the device.
          example: iPhone12
          x-go-name: DeviceName
        deviceType:
          type: string
          description: DeviceType is the type of the device.
          example: web
          x-go-name: DeviceType
        lastAuthenticated:
          type: integer
          description: |-
            LastAuthenticated is the last time the user device authenticated. It's in Unix time which is the number of
            seconds elapsed since January 1, 1970 UTC.
          format: int64
          example: 1666394795
          x-go-name: LastAuthenticated
        pushEnabled:
          type: boolean
          description: PushEnabled is true if the user device has a push token.
          example: true
          x-go-name: PushEnabled
        supportedUvLevel:
          type: array
          description:
            SupportedUvLevel indicates what user verification levels are
            supported on the device.
          example:
            - 0
            - 1
          items:
            type: integer
            format: int64
          x-go-name: SupportedUvLevel
      x-go-name: UserDeviceInfo
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_info
    UserInfoRequest:
      required:
        - requestId
        - userId
      type: object
      properties:
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
        userId:
          type: string
          description: |-
            UserID is a user identifier generated on the customer side to be associated with a device.
            Acceptable characters are: alphanumeric with symbols '-._+=/:'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: UserID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_info
    UserInfoResponse:
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        userDevices:
          type: array
          description: UserDevices contains the list of user device records.
          items:
            $ref: "#/components/schemas/UserInfoDeviceResponse"
          x-go-name: UserDevices
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_info
    UserMobileActiveRequest:
      required:
        - authId
        - requestId
        - userId
      type: object
      properties:
        authId:
          type: string
          description: AuthID is the unique ID of the auth flow.
          example: 713189b8-5555-4b08-83ba-75d08780aebd
          x-go-name: AuthID
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
        userId:
          type: string
          description: |-
            UserID is a user identifier generated on the customer side to be marked as active on a device.
            Acceptable characters are: alphanumeric with symbols '-._+=/:'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: UserID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_mobileactive
    UserMobileActiveResponse:
      required:
        - success
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        success:
          type: boolean
          description: Success is true if the operation was successful.
          example: true
          x-go-name: Success
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_mobileactive
    UserRemoveRequest:
      required:
        - requestId
        - userId
      type: object
      properties:
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
        userId:
          type: string
          description: |-
            UserID is a user identifier generated on the customer side to inactive with associated devices.
            Acceptable characters are: alphanumeric with symbols '-._+=/:'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: UserID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_remove
    UserRemoveResponse:
      required:
        - success
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        success:
          type: boolean
          description: Success is true if the operation was successful.
          example: true
          x-go-name: Success
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_remove
    UserUnbindRequest:
      required:
        - requestId
        - userId
      type: object
      properties:
        deviceId:
          type: string
          description: |-
            DeviceID is the UUID of the device to disassociate from the user ID. If no device is specified, all devices
            associated with the user ID will be unbound.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: DeviceID
        requestId:
          type: string
          description: |-
            RequestID is a UUID generated on the customer side to be associated with the unique request for tracking.
            Acceptable characters are: alphanumeric with symbols '-._+=/'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          default: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: RequestID
        userId:
          type: string
          description: |-
            UserID is a user identifier generated on the customer side to remove association with a device.
            Acceptable characters are: alphanumeric with symbols '-._+=/:'.
          example: eba12f3a-5555-47bc-b85d-21c0cbc4b973
          x-go-name: UserID
      x-go-name: Request
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_unbind
    UserUnbindResponse:
      type: object
      properties:
        error:
          $ref: "#/components/schemas/Error"
        success:
          type: boolean
          x-go-name: Success
      x-go-name: Response
      x-go-package: gitlab.com/prove-identity/prove-auth/garfield/pkg/server/user_unbind
  responses:
    AuthContinueResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AuthContinueResponse"
    AuthExchangeResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AuthExchangeResponse"
    AuthFinishResponse:
      description: Result of the auth flow.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AuthFinishResponse"
    AuthStartResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/AuthStartResponse"
    CardEligibilityResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/CardEligibilityResponse"
    DeviceUnregisterResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/DeviceUnregisterResponse"
    Error400Response:
      description:
        Bad Request. The server cannot process the request due to a client
        error.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Error500Response:
      description:
        Internal Server Error. The server encountered an unexpected condition
        that prevented it from fulfilling the request.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    PushTokenDeleteResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/PushTokenDeleteResponse"
    UserInfoResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/UserInfoResponse"
    UserMobileActiveResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/UserMobileActiveResponse"
    UserRemoveResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/UserRemoveResponse"
    UserUnbindResponse:
      description: Result of the operation.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/UserUnbindResponse"
  securitySchemes:
    token:
      type: apiKey
      description: Enter your API key prefixed by the word "Bearer".
      name: Authorization
      in: header
x-original-swagger-version: "2.0"
